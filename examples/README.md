# Examples

* `quick_scan.py` – scan the NASDAQ-100 for live setups, print explanations, save charts + HTML report.
* `config.strict.json` – tighter tolerances; use with `python -m algovision scan --config examples/config.strict.json`.
